# Example Custom Command

This is an example slash command. You can invoke it with `/example`.

Replace this content with your own instructions for what Claude should do when this command is run.

## Usage

```
/example
```

## What it does

When you run `/example`, Claude will follow the instructions in this file.

You can:
- Ask Claude to run specific analyses
- Generate boilerplate code
- Run project-specific workflows
- Anything else you need repeatedly